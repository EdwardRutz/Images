<p id="notice"><%= notice %></p>

<%= image_tag(@snippet.image_url.to_s) %>

<p>
  <strong>Title:</strong>
  <%= @snippet.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @snippet.content %>
</p>

<%= link_to 'Edit', edit_snippet_path(@snippet) %> |
<%= link_to 'Back', snippets_path %>
